# Experimental

Optional. Specify a known experimental structure for comparison.

```ini
[experimental_structure]
path = ./experimental.cif
```

`path` : `str` | default = `""`.
: Path to the experimental structure file. Leave empty if unknown.
